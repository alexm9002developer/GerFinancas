@model IEnumerable<GerFinancas.Models.TipoLancamento>

@{
    ViewData["Title"] = "Tipos de Lançamento";
}

<h1>Tipo de Lançamento</h1>

@if (TempData["MensagemErro"] != null)
{
    <div class="alert alert-danger" role="alert">
        <button type="button" class="btn btn-danger btn-sm close-alert" data-bs-dismiss="alert" aria-label="Close">X</button>
        @TempData["MensagemErro"]
    </div>
}
@if (TempData["MensagemSucesso"] != null)
{
    <div class="alert alert-success" role="alert">
        <button type="button" class="btn btn-danger btn-sm close-alert" aria-label="Close">X</button>
        @TempData["MensagemSucesso"]
    </div>
}

<p>
    <a asp-controller="TipoLancamento" asp-action="Criar" class="btn btn-primary">Inserir novo tipo</a>
</p>
<table class="table" id="TableTipoLancamento">
    <thead>
        <tr>
            <th>
                Descrição
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descricao)
                </td>
                <td>
                    <a asp-controller="TipoLancamento" asp-action="Editar" asp-route-Codigo="@item.Codigo">Alterar</a> |
                    <a asp-controller="TipoLancamento" asp-action="Detalhes" asp-route-Codigo="@item.Codigo">Visualizar</a> |
                    <a asp-controller="TipoLancamento" asp-action="Apagar" asp-route-Codigo="@item.Codigo">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    <a class="btn btn-secondary" asp-controller="Home" asp-action="Index" role="button">Voltar</a>
</div>
